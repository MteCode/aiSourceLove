# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# build output
dist/

# unplugin 自动生成的类型声明，每次 dev/build 会重新产出
apps/admin/src/auto-imports.d.ts
apps/admin/src/components.d.ts

# uni-app 构建产物
apps/client/dist/
apps/client/unpackage/

# ───────────────────────────────────────────────
# 真实客户资料，严禁入库
# 505 张真人照片 + 相亲档案 PostgreSQL dump。
# 这些是可识别到具体自然人的个人信息，一旦提交并推到 GitHub 就是
# 不可逆的泄露（git 历史里删不干净）。导入用的中间产物一并挡掉。
# ───────────────────────────────────────────────
/photos/
/photo.tar
/xiangqin.dump
/xiangqin*.sql
/import-tmp/

# Claude Code 个人权限配置，不进仓库
.claude/settings.local.json

# 从生产拉下来的备份，含真实客户资料，绝不能进仓库
.backups/
