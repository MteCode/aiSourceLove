# 缘桥小程序 — 功能需求

> 这份文档定义「要做成什么」，[进度.md](进度.md) 记录「做到哪一步」。两者分工不要混。
>
> 标记：`✅ 已实现并验证` · `🔨 部分实现` · `❌ 未实现` · `⏸ 第一版不做`

---

## 一、角色

系统里有四种人，权限和入口都不同：

| 角色 | 是谁 | 从哪进 |
|---|---|---|
| **客户（会员）** | 来找对象的人 | 小程序，手机号注册 |
| **红娘** | 拉人、代录资料、牵线、拿分润 | 同一个小程序，身份升级后出现红娘专区 |
| **运营/审核** | 审档案、审照片、审红娘 | 后台管理端（Web） |
| **超管** | 配置字段、权限、套餐 | 后台管理端（Web） |

### 为什么不做成两个小程序

你提到「分为红娘版本和客户版本」。我的建议是**一个小程序、两套界面**，理由：

1. **审核成本翻倍** —— 两个小程序要两个 appid、两次类目审核、两份婚恋资质。资质本来就是当前最大的卡点。
2. **红娘本身也是用户** —— 她要看档案、看广场、看会员详情，这些页面和客户端完全一样。拆两个就得维护两份。
3. **身份会变** —— 客户申请成为红娘是产品设计里的路径（`pages/matchmaker/apply`）。两个小程序意味着他得换一个小程序用，体验断裂。

**做法**（实施时发现的约束，方案已调整）：

微信小程序的 tabBar **在运行时不能改 pagePath** —— `wx.setTabBarItem` 只能改文字和图标，
改不了跳转目标。想要两套完全不同的标签页，只能走 custom tabBar，
那要给每个页面挂自定义组件，是一层不小的复杂度和 bug 面。

而且盘下来，四个标签**两种角色都需要**：红娘同样要逛广场、用 AI 推荐给客户找对象、
看牵线进度。真正只属于红娘的是工作台、我的会员、发起牵线、收益。

所以最终方案是：**共用四个标签，角色差异体现在「我的」页**——
红娘看到工作台入口，客户看到专属红娘名片。牵线页按角色显示不同视角。
既做到了功能隔离，又不用背 custom tabBar 的复杂度。

---

## 二、客户端功能

### 2.1 注册登录

| 功能 | 状态 | 说明 |
|---|---|---|
| 手机号 + 验证码登录 | ✅ | 未注册自动创建账号 |
| 验证码限流 | ✅ | 60 秒冷却、每日次数上限、5 分钟有效期 |
| 微信一键登录 | ⏸ | 后端已实现，缺 appid/secret，开关已关 |
| 用户协议勾选 | ✅ | 不勾无法提交 |
| **扫红娘分享进来自动归属** | ✅ | 见 §四，四种来路已实测 |

### 2.2 我的资料

| 功能 | 状态 | 说明 |
|---|---|---|
| 填写基本信息 | ✅ | 字段由后台字典驱动，加字段不用发版 |
| 择偶要求 | ✅ | 年龄/身高/学历/收入/婚史/子女/房车 |
| 上传照片 | ✅ | 自动生成打码版；照片单独审核 |
| 提交审核 | ✅ | 状态机：草稿 → 待审 → 通过/驳回 |
| 查看审核记录 | ✅ | 驳回理由可见 |
| 认领已有档案 | 🔨 | 页面在，但存量档案没手机号，认不上（见进度.md） |

### 2.3 找对象

| 功能 | 状态 | 说明 |
|---|---|---|
| 缘分广场 | ✅ | 浏览异性档案，分页 |
| AI 推荐 | ✅ | 三层匹配，每条带推荐理由（DeepSeek 生成） |
| 查看 TA 的资料 | ✅ | 按隐私等级投影，没权限的字段显示为锁 |
| 解锁联系方式 | ✅ | 消耗权益，解锁后可见手机号/微信 |
| 谁看过我 | ✅ | VIP 权益 |
| 举报/拉黑 | ❌ | **建议第一版就做**，见 §六 |

### 2.4 牵线

| 功能 | 状态 | 说明 |
|---|---|---|
| 我的牵线列表 | ✅ | 红娘发起的撮合工单 |
| 牵线详情 | ✅ | 9 状态流转，步骤条展示 |
| 同意/拒绝 | ✅ | 双方都同意才交换联系方式 |
| 反馈见面结果 | ✅ | 已见面 → 成功/失败 |

### 2.5 会员与订单

| 功能 | 状态 |
|---|---|
| 开通 VIP、我的权益、我的订单 | ⏸ 第一版关闭（开关 `VITE_FEATURE_PAY`） |

---

## 三、红娘端功能

红娘登录后，「我的」页出现红娘专区入口。**这些页面客户看不到**。

### 3.1 入驻

| 功能 | 状态 | 说明 |
|---|---|---|
| 申请成为红娘 | ✅ | 填资料提交，后台审核 |
| 审核状态展示 | ✅ | 待审/已通过/已停用 |

### 3.2 工作台

| 功能 | 状态 | 说明 |
|---|---|---|
| 业绩概览 | ✅ | 名下会员数、牵线数、成功数、待结算分润 |
| 我的会员 | ✅ | 名下客户列表 |
| 代客户录入资料 | 🔨 | 后端支持（`ProfileSource.MATCHMAKER`），小程序端入口缺失 |
| 发起牵线 | ✅ | 选两个人，看契合度，创建工单 |
| 两人契合度 | 🔨 | 后端接口在（`/match/score-pair`），小程序端没有页面 |

### 3.3 拉新与分润

| 功能 | 状态 | 说明 |
|---|---|---|
| **分享小程序拉客户** | ✅ | 工作台「分享给客户」按钮，`onShareAppMessage` |
| 我的分润流水 | ✅ | 含冷静期状态 |
| 申请提现 | 🔨 | 后端完整，小程序端页面待确认 |

---

## 四、【重点】分享归属链路

你提的这条需求，后端已经全通，前端一行没写。

### 已实现（2026-08-17）

```
红娘工作台「分享给客户」按钮（open-type=share）
   → 分享路径 /pages/login/index?mm=<matchmakerId>
   → App.onLaunch 捕获 query，存本地（7 天有效）
   → 注册时随 smsLogin 带上
   → 后端校验红娘 ACTIVE，写 user.inviteMatchmakerId
   → 建档案时转写成 profile.matchmakerId
```

线上实测四种来路：

| 来路 | 归属红娘 | 可申请当红娘 |
|---|---|---|
| 红娘分享 | ✅ 已绑定 | ✗ |
| 管理员邀请码 | — | ✅ |
| 自主注册 | — | ✗ |
| 两者都带 | ✅ 红娘优先 | ✗ |

外加「先到先得」实测：老用户再带别的红娘码登录，归属**不变**。

### 设计要点

1. **红娘侧生成分享**
   工作台加「分享给客户」按钮，`onShareAppMessage` 返回带自己 id 的路径：
   `/pages/login/index?mm=<matchmakerId>`
   同时支持生成小程序码（后端调微信接口，需 appid）。

2. **客户侧接收并暂存**
   小程序冷启动时 `App.onLaunch(options)` 拿 `options.query.mm`，存进本地。
   **必须暂存而不是立刻用**：用户点进来可能先逛半天才注册，中途还可能杀掉小程序重进。
   加过期时间（建议 7 天），避免半年前的分享还在生效。

3. **注册时带上**
   `authApi.smsLogin` 增加 `inviteMatchmakerId` 参数，注册成功后清除本地暂存。

4. **归属规则要定死**（这条得你拍板）
   - 已经有归属红娘的老客户，扫了别的红娘的码，算谁的？
     → 建议：**不改**，先到先得。否则会出现红娘互相撬客户。
   - 分享链接过期后注册，算谁的？
     → 建议：算无主，由运营在后台分配。

---

## 五、第一版范围（不含支付）

**做**：注册登录、分享归属、填资料、审核、广场、AI 推荐、看资料、解锁、牵线全流程、红娘工作台、分润查看。

**不做**：VIP 开通、订单、提现打款、微信一键登录。

**验收标准**：
1. 新客户扫红娘分享码 → 注册 → 填资料 → 提交审核 → 后台审核通过 → 出现在广场，且档案归属正确挂在该红娘名下。
2. 红娘登录 → 工作台看到这个新客户 → 发起牵线 → 双方同意 → 交换联系方式 → 标记见面 → 标记成功。
3. 全程无白屏、无报错、无死链接。

---

## 六、建议补充的功能

这几条不在原计划里，但我认为第一版就该有：

1. **举报 / 拉黑**（客户端）
   婚恋产品必备。没有举报入口，一旦出现骚扰或虚假资料，用户只能卸载。
   微信审核也会看这个。

2. **档案「已脱单」自我下架**（客户端）
   现在只有管理员能下架。成了的人没法自己关掉，会持续收到牵线打扰。

3. **红娘端「客户跟进备注」**
   红娘手上几十号人，靠记忆撮合不现实。一个自由文本备注字段能解决大部分问题。

4. ~~**客服 / 联系红娘入口**（客户端）~~ ✅ 已做
   「我的」页显示专属红娘名片，点「联系」直接拨号。
   归属优先取档案上的 matchmakerId（运营可能在后台改过），没档案才回落到注册时的暂存。
   红娘已停用时不露出，让运营重新分配。

---

## 七、和后台管理端的分工

小程序不做的，后台做：审核档案与照片、审核红娘入驻、分配归属红娘、配置字段字典、配置 VIP 套餐、权限管理、操作日志、支付对账、数据看板。
