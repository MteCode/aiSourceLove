# 生产环境。
#
# H5 和后端同域，由 Nginx 反代 /api，用相对路径即可。
# 小程序没有「同域」这回事，也不认相对路径，必须是完整的 https 绝对地址，
# 而且这个域名要在微信公众平台「开发管理 → 服务器域名 → request 合法域名」里配过。
#
# 所以两端不能共用同一个基址：打 H5 用 VITE_API_BASE，打小程序用 VITE_API_BASE_MP。
# 具体取哪个由 src/api/request.ts 按平台判断，见那里的注释。
VITE_API_BASE=/api

# 小程序专用。换域名只改这一行，然后重新 npm run build:mp。
VITE_API_BASE_MP=https://flarexcode.com/api
